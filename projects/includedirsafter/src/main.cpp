#include <header.h> // included thanks to includedirsafter { "includedirsafter" }

#include "vector" // uses system directories (so for std::vector<T>)
#include <vector> // uses system directories (so for std::vector<T>)

int main()
{
}
