#include "pch.h"
// Code before the pch included is ignored by msvc,
// so we cannot have conditional pch with MSVC.
// "portable" way is to include pch fist anyway
// non-msvc will look for corresponding gch

#include "../content/foo.content"
